# Scamicide Environment Variables
# Copy this file to .env and fill in the values

# Server Configuration
PORT=3001
NODE_ENV=development

# Database (auto-configured for Render with persistent disk)
# In production on Render set DB_PATH to the mounted disk path: /data/scamicide.db
# (Make sure to add a persistent disk in Render Dashboard and mount it at /data)
DB_PATH=./data/scamicide.db

# Frontend URL (for CORS)
# In development: http://localhost:5173
# In production: https://your-frontend.vercel.app
FRONTEND_URL=http://localhost:5173

# API Configuration
# VITE_API_URL - set this in frontend/.env for production

